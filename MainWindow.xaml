<Window x:Class="gc_bot.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gc_bot"
        xmlns:controls="clr-namespace:gc_bot.Controls"
        mc:Ignorable="d"
        Title="gc_bot" Height="600" Width="900">
    <DockPanel>

        <Menu DockPanel.Dock="Top" IsMainMenu="True">
            <MenuItem Header="_File">
                <MenuItem Header="_New" Click="New_Click" />
                <MenuItem Header="_Open..." Click="Open_Click" />
                <Separator />
                <MenuItem Header="E_xit" Click="Exit_Click" />
            </MenuItem>

            <MenuItem Header="_Edit">
                <MenuItem Header="_Preferences..." Click="Preferences_Click" />
            </MenuItem>

            <MenuItem Header="_Help">
                <MenuItem Header="_About" Click="About_Click" />
            </MenuItem>
        </Menu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Top: externalized control -->
            <Border Grid.Row="0" Padding="8" BorderBrush="LightGray" BorderThickness="0,0,0,1">
                <controls:TopButtons x:Name="TopButtonsControl"/>
            </Border>

            <!-- Bottom: externalized list control bound to MainWindow.Items -->
            <Border Grid.Row="1" Padding="8">
                <controls:ItemsPanel x:Name="ItemsPanelControl"
                                     ItemsSource="{Binding Items, RelativeSource={RelativeSource AncestorType=Window}}"/>
            </Border>
        </Grid>
    </DockPanel>
</Window>
